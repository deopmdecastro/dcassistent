# Sub-diretório do subsistema de rede aplicacional (HTTP/WebSocket).
# O CMakeLists de topo em `firmware/main/CMakeLists.txt` inclui estas
# fontes explicitamente — este ficheiro existe apenas para dar contexto
# a IDEs / geradores.
