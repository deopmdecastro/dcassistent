dependencies:
  idf: ">=5.3"
  lvgl/lvgl: "^9.2"
  espressif/esp_lvgl_port: "^2.9"
  espressif/esp_lcd_ili9341: "^2.0.1"
  espressif/led_strip: "^3.0"
  # DC 0.3 — touch (ES3C28P) e áudio (ES8311). Versões por confirmar contra o
  # Component Registry no primeiro `idf.py build` (sem acesso à rede neste
  # ambiente de auditoria/dev, não foi possível validar os números exatos).
  espressif/esp_lcd_touch: "^1.1.0"
  espressif/esp_lcd_touch_ft6336: "^1.0.5"
  espressif/esp_codec_dev: "^1.3.1"
